# 安全策略

## 报告安全问题

如果发现凭证泄露、命令执行、路径遍历、敏感信息输出或其他安全问题，请不要公开创建 issue。

请通过 GitHub Security Advisory 或仓库维护者提供的私密联系方式报告，并包含：

- 影响版本或 commit。
- 复现步骤。
- 预期影响。
- 建议修复方向。

## 项目安全边界

当前版本只读取本地 JSON 文件并输出文本报告，不主动访问网络，也不执行外部命令。后续如果加入 GitHub API 或 AI provider，需要新增配置脱敏、超时、速率限制和测试。
